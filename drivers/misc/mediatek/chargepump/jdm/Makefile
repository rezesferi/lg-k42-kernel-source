# Add CHARGEPUMP driver for JDM
# # Add driver list what you want to use in CUSTOM_KERNEL_CHARGEPUMP_LIST

JDM_CHARGEPUMP_LISTS := $(subst ",,$(CONFIG_CUSTOM_KERNEL_CHARGEPUMP_LIST))
obj-$(CONFIG_CUSTOM_KERNEL_CHARGEPUMP) += $(foreach CHARGEPUMP,$(JDM_CHARGEPUMP_LISTS),$(CHARGEPUMP)/)
